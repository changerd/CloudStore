@model IEnumerable<CloudStore.Models.Order>

@{
    ViewBag.Title = "Orders";
}

<h2>Orders</h2>

<div class="row panel">
    <div class="col-md-2">
        <form method="get">
            <div class="form-inline">
                <label class="control-label">Статус</label>
                <p>@Html.DropDownList("status", ViewBag.StatusOrder as SelectList, htmlAttributes: new { @class = "form-control" })</p>
                <input type="submit" value="Фильтр" class="btn btn-default" />

            </div>
        </form>
    </div>
    <div class="col-md-10">
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayName("Order number")
                </th>
                <th>
                    @Html.DisplayName("Client login")
                </th>
                <th>
                    @Html.DisplayName("Client name")
                </th>
                <th>
                    @Html.DisplayName("Status")
                </th>
                <th>
                    @Html.DisplayName("Price")
                </th>
                <th>
                    @Html.DisplayName("Date")
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cart.User.UserName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cart.User.FullName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.StatusOrder.StatusOrderName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Value) руб
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Date)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "EditOrder", new { id = item.Id }) |
                        @Html.ActionLink("Details", "DetailsOrder", new { id = item.Id }) |
                        @Html.ActionLink("Delete", "DeleteOrder", new { id = item.Id })
                    </td>
                </tr>
            }

        </table>
    </div>
</div>

<div>
    @Html.ActionLink("Назад", "Index")
</div>
